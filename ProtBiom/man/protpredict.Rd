% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction_functions.R
\name{protpredict}
\alias{protpredict}
\alias{predictProtFI}
\alias{predictProtMort}
\title{Protein-based score prediction (case-insensitive matching)}
\usage{
protpredict(
  data,
  biomarker = c("ProtFI", "ProtMort", "custom")[1],
  id_col = NULL,
  id_name = "sampleid",
  scale = FALSE,
  coef_df = NULL,
  highcor_df = NULL
)

predictProtFI(
  data,
  id_col = NULL,
  scale = FALSE,
  coef_df = NULL,
  highcor_df = NULL
)

predictProtMort(
  data,
  id_col = NULL,
  scale = FALSE,
  coef_df = NULL,
  highcor_df = NULL
)
}
\arguments{
\item{data}{A data.frame with participants (rows).}

\item{biomarker}{Character. "ProtFI", "ProtMort", or a custom label.
If "ProtFI"/"ProtMort", coefficients are auto-loaded from package data
(\code{ProtFIcoef_tbl} / \code{ProtMortcoef_tbl}) and custom \code{coef_df} is not allowed.
For any other value, you must provide \code{coef_df}.}

\item{id_col}{Optional input ID column name in \code{data} (case-insensitive). If NULL, the function
will try to auto-detect a column named "sampleid" (case-insensitive). If still not found,
no ID column is returned.}

\item{id_name}{Output ID column name (default "sampleid").}

\item{scale}{Logical; if TRUE, z-scale used predictors with mean/SD from \code{data}.}

\item{coef_df}{Optional override for coefficients (columns "Protein","Coefficient").}

\item{highcor_df}{Optional override for fallback map (defaults to package data \code{highcor_tbl},
columns "Protein","Strongest correlated protein").}
}
\value{
A data.frame with the ID column (if available, named by \code{id_name}) and a score column
named exactly as provided in \code{biomarker}.
}
\description{
Rules:
\itemize{
\item Coefficients are chosen by \code{biomarker} or taken from \code{coef_df}.
\item Missing predictors are replaced by their strongest correlate from \code{highcor_tbl},
except 'age': if 'age' is missing in \code{data} (and present in \code{coef_df}), error.
\item If \code{scale = TRUE} and any predictor contains NA, error (expects imputed data; original paper used KNN with k = 5).
\item If \code{scale = FALSE}, predictors must already be z-standardized (~ mean 0, sd 1); otherwise error advising \code{scale = TRUE}.
\item With \code{scale = FALSE}, NA values propagate to the score (no internal imputation).
\item Matching of column names is case-insensitive.
}
}
